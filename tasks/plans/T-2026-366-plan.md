# Plan: T-2026-366 — Wire EmptyStateComponent into LevelSelectPage for no-level-data state

## Task
LevelSelectPage displays levels grouped by tier, but when level data has not been registered for a game, the player sees either a bare `<p>` tag or blank content. Wire EmptyStateComponent to provide a polished empty state with a "Back to Minigames" navigation button.

## Architecture
- Replace the existing `@empty` block (line 86-88 in level-select.ts) with `<nx-empty-state>` usage
- Inject `MinigameRegistryService` to resolve the human-readable game name from the `gameId` route param
- Add a computed signal `gameName` that looks up the registry config
- Add a `navigateToMinigames()` method for the "Back to Minigames" button
- Use the `map` icon (already registered in APP_ICONS) for the empty state

## Files to Create/Modify
| File | Action | Purpose |
|------|--------|---------|
| src/app/pages/level-select/level-select.ts | modify | Import EmptyStateComponent, inject MinigameRegistryService, add gameName computed, replace @empty block |
| src/app/pages/level-select/level-select.spec.ts | modify | Update test #17 for new empty state, add test for game name display, add test for back button navigation |

## Test Plan
1. Test: empty state renders `nx-empty-state` when no levels (replaces test #17)
2. Test: empty state title contains "Levels coming soon"
3. Test: empty state message contains the game name from registry
4. Test: "Back to Minigames" button exists and navigates to `/minigames`
5. Test: empty state is NOT shown when levels exist (existing tests already cover this implicitly)

## Implementation Order
1. **Red** — Update test #17 and add new tests for empty state behavior (tests will fail because component still uses `<p>`)
2. **Green** — Modify level-select.ts: add EmptyStateComponent import, inject MinigameRegistryService, add gameName computed, replace @empty block with nx-empty-state
3. **Refactor** — Clean up, verify all tests pass

## Verification
- [ ] `npx ng build` compiles without errors
- [ ] `npx ng test --watch=false` all tests pass
- [ ] Empty state shows `nx-empty-state` with "map" icon when no levels
- [ ] Empty state title: "Levels coming soon"
- [ ] Empty state message includes game name (e.g., "Module Assembly")
- [ ] "Back to Minigames" button navigates to `/minigames`
- [ ] Existing level display tests still pass (no regression)
